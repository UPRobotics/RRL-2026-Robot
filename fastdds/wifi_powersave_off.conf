# NetworkManager — disable WiFi power saving permanently
# Copy to /etc/NetworkManager/conf.d/ and restart NetworkManager:
#   sudo cp fastdds/wifi_powersave_off.conf /etc/NetworkManager/conf.d/
#   sudo systemctl restart NetworkManager

[connection]
wifi.powersave = 2
